# bubble-starter-data-redis

## 作用

- 统一 Redis 客户端与配置（可结合 Redisson）。

## 依赖引入

```xml
<dependency>
  <groupId>cn.fxbin.bubble</groupId>
  <artifactId>bubble-starter-data-redis</artifactId>
</dependency>
```

## 版本对齐

- 以 BOM 中 `redisson.version` 为准。

